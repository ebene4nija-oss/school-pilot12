<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Every permission here is asked for in context, at the moment it is
        needed, and every one of them degrades gracefully if refused.

        Note what is absent: no biometric permission, no NFC, no Bluetooth.
        Attendance is the phone camera and phone GPS, and that is a product
        rule, not an oversight (doc §3).
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Staff clock-in, checked against the school geofence server-side. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- QR attendance and homework photo uploads. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
        Android 13+ requires this to be granted at runtime. Asked for at
        sign-in rather than at launch, and refusing it costs the user nothing
        they cannot get another way — the school still reaches them by SMS.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        The camera is genuinely optional: a teacher without one can still take
        a register by tapping. Marking it required would keep the app off
        handsets that can do everything except scan.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:label="SchoolPilot"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
            The channel a notification lands in when the FCM payload names one
            the device has not seen. It must match `PushService.androidChannelId`
            and the backend's `FCM_ANDROID_CHANNEL_ID` — Android 8+ silently
            drops a notification addressed to an unknown channel, with no error
            anywhere, which is a bug that looks exactly like "push is broken".
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="schoolpilot_default" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
